[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  PUPPETEER_SKIP_DOWNLOAD = "false"
  PUPPETEER_CACHE_DIR = "/opt/build/repo/.cache/puppeteer"

# Cache the Chromium download between builds so we don't re-download
# ~150 MB every deploy.
[[plugins]]
  package = "netlify-plugin-cache"

  [plugins.inputs]
    paths = [
      ".cache/puppeteer"
    ]
